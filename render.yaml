services:
  - type: web
    name: attachment-api
    env: python
    plan: free
    region: oregon
    buildCommand: cd attachment_api && pip install -r requirements.txt
    startCommand: cd attachment_api && python -m gunicorn config.wsgi:application --log-file -
